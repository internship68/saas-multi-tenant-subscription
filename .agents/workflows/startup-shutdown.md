---
description: วิธีการเริ่มและหยุดระบบพัฒนา (Startup & Shutdown Workflow)
---

### 🛑 วิธีการหยุดระบบ (Shutdown)
หากคุณต้องการปิดคอมพิวเตอร์หรือหยุดทำงานชั่วคราว ให้ทำตามขั้นตอนดังนี้:

1. **หยุด Backend**: ไปที่ Terminal ที่รัน NestJS แล้วกด `Ctrl + C`
2. **หยุด Frontend**: ไปที่ Terminal ที่รัน Next.js แล้วกด `Ctrl + C`
3. **หยุด Stripe CLI**: ไปที่ Terminal ที่รัน Stripe Listen แล้วกด `Ctrl + C`
4. **ปิด Database** (ถ้าคุณใช้ Local Docker): รันคำสั่ง `docker-compose down` (ถ้ามี)

---

### 🚀 วิธีการเริ่มระบบใหม่ (Startup)
เมื่อคุณกลับมาทำงานอีกครั้ง ให้เปิด Terminal แยกกัน 3 หน้าต่าง และรันคำสั่งตามลำดับดังนี้:

#### 1. เริ่มระบบ Backend
เปิด Terminal ในโฟลเดอร์: `d:\saas-multi-tenant-subscription\saas-platform\backend`
// turbo
```bash
npm run start:dev
```

#### 2. เริ่มระบบ Frontend
เปิด Terminal ในโฟลเดอร์: `d:\saas-multi-tenant-subscription\saas-platform\frontend`
// turbo
```bash
npm run dev
```

#### 3. เริ่มระบบ Stripe Webhook (สำหรับการทดสอบการชำระเงิน)
เปิด Terminal ในโฟลเดอร์: `d:\saas-multi-tenant-subscription`
// turbo
```bash
./stripe_cli/stripe listen --forward-to localhost:3002/webhook/stripe
```

---

### 💡 คำแนะนำเพิ่มเติม
- **Database**: หากคุณใช้ฐานข้อมูลภายนอก (เช่น Supabase หรือ Railway) ไม่ต้องทำอะไรเพิ่มเติม แต่ถ้าใช้ Local DB ตรวจสอบให้แน่ใจว่ามันรันอยู่ก่อนเริ่ม Backend
- **Browser**: เมื่อรันครบแล้ว สามารถเข้าใช้งานได้ที่ http://localhost:3000 (Frontend) และดู API Swagger ได้ที่ http://localhost:3002/api (Backend)
